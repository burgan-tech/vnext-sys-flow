# Example npm configuration for GitHub Packages
# Copy this file to .npmrc and replace YOUR_GITHUB_TOKEN with your actual token

# Configure the registry for @amorphie scoped packages
@amorphie:registry=https://npm.pkg.github.com

# Authentication token for GitHub Packages
# You can use an environment variable for security:
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# Or directly set the token (not recommended for shared repositories):
# //npm.pkg.github.com/:_authToken=ghp_xxxxxxxxxxxxxxxxxxxx

# Always authenticate when accessing the registry
always-auth=true
